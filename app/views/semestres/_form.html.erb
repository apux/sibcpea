<%= form_for(@semestre) do |f| %>
  <%= render 'shared/error_messages', :target => @semestre %>

  <table class="form">
    <thead>
      <tr>
        <td colspan="2">Datos del Semestre</td>
      </tr>
      <tr>
      </tr> 
    </thead>
    <tfoot>
      <tr> 
        <td colspan="2">
          <div class="actions">
            <%= f.submit %>
          </div>
        </td>
      </tr>
      <tr>
      </tr>
    </tfoot>
    <tbody>
      <tr>
        <td class="name">
          <%= f.label :numero %>:
        </td>
        <td class="input">
          <%= f.text_field :numero %>
        </td>
      </tr>
      <tr>
        <td class="name">
          <%= f.label :nombre %>:
        </td>
        <td class="input">
          <%= f.text_field :nombre %>
        </td>
      </tr>
      <tr>
        <td class="name">
          <%= f.label :descripcion %>:
        </td>
        <td class="input">
          <%= f.text_field :descripcion %>
        </td>
      </tr>
    </tbody>
  </table>
<% end %>

<script type="text/javascript" language="javascript">
  var links = $$('.field input');
  links.each(
      function(element) {
        Event.observe(element, 'keyup', function(){
          element.value = element.value.toUpperCase();
        });
      }
      );
</script>
